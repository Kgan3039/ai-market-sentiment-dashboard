[pytest]
# Phase 0 intentionally retires the sentiment/prediction product surface.
# Keep its active API, fixture, configuration, and summarization tests as the
# default suite while legacy backend experiment files remain available for
# historical reference outside the Phase 0 test paths.
testpaths =
    tests
    backend/tests
